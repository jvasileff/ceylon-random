"Run the module `com.vasileff.ceylon.random`." 
shared void run(){
    // Write your code here and afterwards
    // on the command line compile with "ceylon compile com.vasileff.ceylon.random" or "ant compile"
    // and execute with "ceylon compile com.vasileff.ceylon.random/0.0.1" or "ant run"
}
